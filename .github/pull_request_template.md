## What does this PR do?

<!-- Short description of the change and why. Link related issues with "Fixes #N". -->

## Checklist

- [ ] Ran the app from source (`uv run python app.py`) and exercised the change
- [ ] No MarkItDown bundling — conversion still goes through the device CLI
- [ ] UI changes use inline SVG icons from `ICON_PATHS` (no emojis, no image files)
- [ ] Updated `CHANGELOG.md` under an `Unreleased` heading if user-facing
